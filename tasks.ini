# Example tasks configuration for the task-runner TUI.
#
# Format: name = shell command
# Lines starting with '#' and blank lines are ignored.

hello = echo "Hello from the task runner!"
list = ls -la
date = date
